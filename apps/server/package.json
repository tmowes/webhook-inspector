{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@ai-sdk/google": "2.0.26",
    "@fastify/cors": "11.1.0",
    "@fastify/swagger": "9.5.2",
    "@scalar/fastify-api-reference": "1.38.1",
    "ai": "5.0.86",
    "fastify-type-provider-zod": "6.1.0",
    "fastify": "^5.6.1",
    "zod": "catalog:",
    "@webhook-inspector/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "^1.3.1",
    "tsdown": "catalog:"
  }
}
